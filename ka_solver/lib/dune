(library
 (name ka_solver)
 (public_name ka_solver)
 (libraries core)
 (preprocess
  (pps ppx_sexp_conv))
 (modules
  expr
  expr_with_plus_subterms
  expr_with_times_subterms
  expr_set
  equivalence_checker
  parser
  lexer))

(menhir
 (modules parser))

(ocamllex lexer)
